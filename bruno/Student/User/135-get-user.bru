meta {
  name: GET user
  type: http
  seq: 135
}

get {
  url: {{baseUrl}}/user
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/user
  Action: Closure
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
