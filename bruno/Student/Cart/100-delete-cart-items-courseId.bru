meta {
  name: DELETE cart items courseOfferingId
  type: http
  seq: 100
}

delete {
  url: {{baseUrl}}/cart/items/{{courseOfferingId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/cart/items/{courseId}
  Action: App\Http\Controllers\Api\CartController@removeItem
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
