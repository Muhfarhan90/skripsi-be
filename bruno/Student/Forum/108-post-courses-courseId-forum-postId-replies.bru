meta {
  name: Create Forum Reply
  type: http
  seq: 108
}

post {
  url: {{baseUrl}}/courses/{{courseId}}/forum/{{postId}}/replies
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/courses/{courseId}/forum/{postId}/replies
  Action: App\Http\Controllers\Api\ForumController@storeReply
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
