meta {
  name: Create Forum Post
  type: http
  seq: 104
}

post {
  url: {{baseUrl}}/courses/{{courseId}}/forum
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/courses/{courseId}/forum
  Action: App\Http\Controllers\Api\ForumController@store
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
