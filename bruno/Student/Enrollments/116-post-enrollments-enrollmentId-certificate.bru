meta {
  name: POST enrollments enrollmentId certificate
  type: http
  seq: 116
}

post {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/certificate
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/enrollments/{enrollmentId}/certificate
  Action: App\Http\Controllers\Api\CertificateController@generate
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
