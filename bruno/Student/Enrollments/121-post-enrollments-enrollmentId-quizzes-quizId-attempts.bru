meta {
  name: POST enrollments enrollmentId quizzes quizId attempts
  type: http
  seq: 121
}

post {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/quizzes/{{quizId}}/attempts
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/enrollments/{enrollmentId}/quizzes/{quizId}/attempts
  Action: App\Http\Controllers\Api\QuizAttemptController@store
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
