meta {
  name: GET enrollments enrollmentId lesson-progress
  type: http
  seq: 117
}

get {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/lesson-progress
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/enrollments/{enrollmentId}/lesson-progress
  Action: App\Http\Controllers\Api\LessonProgressController@index
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
