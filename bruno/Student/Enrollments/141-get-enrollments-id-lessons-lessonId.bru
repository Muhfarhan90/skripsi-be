meta {
  name: GET enrollments id lessons lessonId
  type: http
  seq: 141
}

get {
  url: {{baseUrl}}/enrollments/{{id}}/lessons/{{lessonId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/enrollments/{id}/lessons/{lessonId}
  Action: App\Http\Controllers\Api\EnrollmentController@lessonDetail
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
  Response Notes: Lesson detail dapat berasal dari live master atau snapshot fallback, dan menyertakan flag is_supplemental, counts_toward_progress, counts_toward_certificate, source, is_locked, is_new, dan is_completed.
}
