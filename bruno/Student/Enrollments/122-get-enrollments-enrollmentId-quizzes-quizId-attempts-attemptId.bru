meta {
  name: GET enrollments enrollmentId quizzes quizId attempts attemptId
  type: http
  seq: 122
}

get {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/quizzes/{{quizId}}/attempts/{{attemptId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/enrollments/{enrollmentId}/quizzes/{quizId}/attempts/{attemptId}
  Action: App\Http\Controllers\Api\QuizAttemptController@show
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
