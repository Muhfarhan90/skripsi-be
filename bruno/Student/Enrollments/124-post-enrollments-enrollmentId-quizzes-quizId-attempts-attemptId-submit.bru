meta {
  name: POST enrollments enrollmentId quizzes quizId attempts attemptId submit
  type: http
  seq: 124
}

post {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/quizzes/{{quizId}}/attempts/{{attemptId}}/submit
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/enrollments/{enrollmentId}/quizzes/{quizId}/attempts/{attemptId}/submit
  Action: App\Http\Controllers\Api\QuizAttemptController@submit
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
