meta {
  name: PUT enrollments enrollmentId lesson-progress lessonId
  type: http
  seq: 119
}

put {
  url: {{baseUrl}}/enrollments/{{enrollmentId}}/lesson-progress/{{lessonId}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/enrollments/{enrollmentId}/lesson-progress/{lessonId}
  Action: App\Http\Controllers\Api\LessonProgressController@upsert
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum
}
