meta {
  name: DELETE courses.destroy
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/admin/courses/{{course}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/admin/courses/{course}
  Action: App\Http\Controllers\Api\Admin\CourseController@destroy
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
