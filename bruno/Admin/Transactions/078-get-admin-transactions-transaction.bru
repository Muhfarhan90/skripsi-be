meta {
  name: GET transactions.show
  type: http
  seq: 78
}

get {
  url: {{baseUrl}}/admin/transactions/{{transaction}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/admin/transactions/{transaction}
  Action: App\Http\Controllers\Api\Admin\TransactionController@show
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
