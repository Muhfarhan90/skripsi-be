meta {
  name: POST admin enrollments id sync-progress
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/admin/enrollments/{{id}}/sync-progress
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/admin/enrollments/{id}/sync-progress
  Action: App\Http\Controllers\Api\Admin\EnrollmentController@syncProgress
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
