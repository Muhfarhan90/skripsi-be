meta {
  name: PUT admin enrollments enrollmentId lesson-progress lessonId
  type: http
  seq: 28
}

put {
  url: {{baseUrl}}/admin/enrollments/{{enrollmentId}}/lesson-progress/{{lessonId}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/admin/enrollments/{enrollmentId}/lesson-progress/{lessonId}
  Action: App\Http\Controllers\Api\Admin\LessonProgressController@upsert
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
