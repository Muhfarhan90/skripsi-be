meta {
  name: PATCH admin enrollments id status
  type: http
  seq: 30
}

patch {
  url: {{baseUrl}}/admin/enrollments/{{id}}/status
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/admin/enrollments/{id}/status
  Action: App\Http\Controllers\Api\Admin\EnrollmentController@updateStatus
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
