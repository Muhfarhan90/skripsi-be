meta {
  name: DELETE quizzes.destroy
  type: http
  seq: 58
}

delete {
  url: {{baseUrl}}/admin/quizzes/{{quiz}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

docs {
  URI: api/admin/quizzes/{quiz}
  Action: App\Http\Controllers\Api\Admin\QuizController@destroy
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
