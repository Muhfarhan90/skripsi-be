meta {
  name: PUT quizzes.update
  type: http
  seq: 60
}

put {
  url: {{baseUrl}}/admin/quizzes/{{quiz}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/admin/quizzes/{quiz}
  Action: App\Http\Controllers\Api\Admin\QuizController@update
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
