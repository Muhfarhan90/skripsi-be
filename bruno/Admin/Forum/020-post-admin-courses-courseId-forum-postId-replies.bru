meta {
  name: Create Forum Reply (Admin)
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/admin/courses/{{courseId}}/forum/{{postId}}/replies
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

docs {
  URI: api/admin/courses/{courseId}/forum/{postId}/replies
  Action: App\Http\Controllers\Api\Admin\ForumController@storeReply
  Middleware: api, Illuminate\Auth\Middleware\Authenticate:sanctum, App\Http\Middleware\EnsureAdminAccess
}
