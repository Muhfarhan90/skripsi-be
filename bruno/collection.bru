docs {
  # Skripsi BE API (Bruno)
  
  Collection ini mengikuti daftar endpoint dari route API Laravel (`php artisan route:list --path=api --except-vendor`).
}

headers {
  Accept: application/json
}
